# 다크 테마 디자인 명세 (GTO Wizard 스타일)

참고 이미지: `C:\Users\하봄\Downloads\클로드-프로그램만들기\참고자료\frame_59.jpg` (및 frame_90/150.jpg)
— 유튜브 영상 속 GTO Wizard 화면. 이 룩앤필을 목표로 한다.

## 팔레트 (Tailwind neutral 스케일 기준)
| 용도 | 클래스 | 비고 |
|---|---|---|
| 앱 배경 | `bg-neutral-900` (#171717) | body 전역 |
| 패널/카드 배경 | `bg-neutral-800` (#262626) | 표, 설정 박스 |
| 상승 요소(호버/선택) | `bg-neutral-700` (#404040) | 사이드바 선택 항목 등 |
| 테두리 | `border-neutral-700` (기본) / `border-neutral-600` (강조) | 밝은 회색 테두리 금지 |
| 본문 텍스트 | `text-neutral-200` | 전역 기본 |
| 보조 텍스트 | `text-neutral-400` | 설명, 단위 |
| 강조(수치·선택) | `text-yellow-400` 또는 `text-blue-400` | 과용 금지 |

## 변환 규칙 (라이트 → 다크)
- `bg-gray-50/100/200/300` → `bg-neutral-800` (패널) 또는 `bg-neutral-700` (버튼형)
- `bg-blue-100` (선택/호버 하이라이트) → `bg-neutral-700`, 필요시 `text-blue-300` 추가
- `bg-red-100`/`bg-red-50` (경고 배경) → `bg-red-950`
- `bg-yellow-200` → `bg-yellow-900`
- `text-black`/`text-gray-700`/`text-gray-600` → `text-neutral-200` / 보조면 `text-neutral-400`
- `text-gray-500` (아이콘) → `text-neutral-400`
- `border-black`/`border-gray-300/400/500/600` → `border-neutral-600` (표 셀 등 촘촘한 곳은 `border-neutral-700`)
- `text-red-500` (에러) → `text-red-400` / `text-orange-500` (경고) → `text-orange-400`
- `text-cyan-600 bg-cyan-50 border-cyan-600` (안내 배너) → `text-cyan-300 bg-cyan-950 border-cyan-700`
- 흰 배경 전제의 `shadow` → 유지하되 `shadow-black/40` 정도로. 어색하면 제거하고 border로 대체
- **이미 어두운 것 유지**: `bg-neutral-800`(레인지 매트릭스 셀), `bg-slate-800`(NavBar), 액션 색(fold 파랑/check·call 초록/bet 앰버→빨강 그라데이션), `yellow500` 레인지 채움색
- 차트(chart.js): 눈금·라벨색이 어두운 배경에서 보이도록 옵션에 `color: "#a3a3a3"`, grid `color: "#404040"` 지정 (해당 파일에 이미 옵션 객체가 있으면 거기에 추가)

## 전역 CSS가 처리해주는 것 (index.css — 컴포넌트에서 중복 지정 불필요)
- body 배경·기본 텍스트색
- input/select/textarea 다크 스타일 (`bg-neutral-800 border-neutral-600 text-neutral-100`)
- 라디오/체크박스 `accent-blue-500`
- `.input-error` 다크 변형
- 스피너 배경색

## 금지
- 기능·로직·번역 문자열 변경 금지. 클래스(스타일)만 수정
- 레이아웃(flex/grid/spacing) 변경 금지 — 색만 바꾼다
- Vue 바인딩 안의 조건부 클래스 문자열은 신중히: 문자열 연결 구조를 깨뜨리지 말 것
